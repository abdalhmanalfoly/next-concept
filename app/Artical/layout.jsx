
export const metadata = {
    title: "Article page",
    description: "Generated",
};

export default function ArticleLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
